services:
  - type: web
    name: pdf-fusion-pro-ultimate
    repo: https://github.com/mybsow/pdf-fusion-pro_ultimate
    region: frankfurt
    plan: free
    branch: main
    autoDeploy: true

    envVars:
      - key: PORT
        value: "10000"
      - key: OCR_ENABLED
        value: "true"

    # ✅ BUILD COMMAND ULTRA SIMPLE ET EFFICACE
    buildCommand: |
      apt-get update
      apt-get install -y tesseract-ocr tesseract-ocr-fra poppler-utils
      pip install --upgrade pip
      pip install -r requirements.txt

    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 1 app:app

    # ✅ Packages système (Render les installera automatiquement)
    aptPackages:
      - tesseract-ocr
      - tesseract-ocr-fra
      - poppler-utils

    healthCheckPath: /health
