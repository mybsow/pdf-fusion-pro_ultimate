services:
  - type: web
    name: pdf-fusion-pro-ultimate
    repo: https://github.com/mybsow/pdf-fusion-pro_ultimate
    region: frankfurt
    plan: free
    branch: main
    autoDeploy: true

    envVars:
      - key: PORT
        value: "10000"
      - key: OCR_ENABLED
        value: "true"

    # ✅ BUILD COMMAND AVEC GITHUB
    buildCommand: |
      echo "🚀 INSTALLATION OCR DEPUIS GITHUB"
      
      # Exécuter le script Python d'installation
      python install_from_github.py
      
      # Ou exécuter le script shell
      # chmod +x render-build.sh
      # ./render-build.sh
      
      # Installer le reste de l'app
      pip install -r requirements.txt
      
      echo "✅ BUILD COMPLET"

    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 1 app:app

    healthCheckPath: /health
    healthCheckTimeout: 30
